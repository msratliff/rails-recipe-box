<div class="grid">

	<% @recipe.each do |recipe| %>
		<div class="grid__container">
			<div class="grid__image">
			<%= link_to image_tag(recipe.image.url(:medium)), recipe %>  
			</div>
			<h2 class="grid__text"><%= link_to recipe.title, recipe %>  </h2>
		</div>
	<% end %>

</div>